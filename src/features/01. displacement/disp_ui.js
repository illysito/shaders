import displacementHandler from './disp_handler'

function displacementUI(canvas) {
  //prettier-ignore
  // const updateUniforms = displacementHandler(canvas)
  displacementHandler(canvas)
}

export default displacementUI
