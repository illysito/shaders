import textureHandler from './texture_handler'

function textureUI(canvas) {
  //prettier-ignore
  // const updateUniforms = displacementHandler(canvas)
  textureHandler(canvas)
}

export default textureUI
